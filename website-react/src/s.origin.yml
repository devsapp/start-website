edition: 1.0.0
services:
  website-starter:
    component: devsapp/website
    access: my
    props:
      bucket: shl-website-test01
      src:
        codeUri: ./
        publishDir: ./build
        buildCommand: npm run build
        index: index.html
        error: index.html
      region: cn-hangzhou
      hosts:
        - host: auto
