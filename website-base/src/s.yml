edition: 1.0.0
services:
  website-starter:
    component: devsapp/website
    access: my
    props:
      bucket: "{{ bucket|bucket存储桶 }}"
      src:
        codeUri: ./
        publishDir: ./build
        buildCommand: npm run build
        index: index.html
        error: index.html
      region: cn-hangzhou
      hosts:
        - host: shl-website-test01.oss.devsapp.cn
