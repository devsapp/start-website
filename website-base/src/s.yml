edition: 1.0.0
access: '{{ access }}'

services:
  website-starter:
    component: devsapp/website
    props:
      bucket: '{{ bucket | alibaba oss bucket }}'
      src:
        publishDir: ./src
        index: index.html
      region: cn-hangzhou
      hosts:
        - host: auto
